part of 'theme_cubit.dart';

class ThemeState {
  Color background;
  Color item;
  Color reverceColor;
  Color shadowColor;
  ThemeState(
      {required this.background,
      required this.item,
      required this.reverceColor,
      required this.shadowColor});
}
